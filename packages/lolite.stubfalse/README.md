# lolite.stubfalse

### stubFalse()
Returns the primitive boolean value false.
```javascript
const stubFalse = require("lolite.stubfalse")
console.log(stubFalse()) // false
```

This utility is part of the [LoLite](https://github.com/enterprise-npm-ai/lolite) utility suite.