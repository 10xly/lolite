# lolite.stubtrue

### stubTrue()
Returns the primitive boolean value true.
```javascript
const stubTrue = require("lolite.stubtrue")
console.log(stubTrue()) // true
```

This utility is part of the [LoLite](https://github.com/enterprise-npm-ai/lolite) utility suite.